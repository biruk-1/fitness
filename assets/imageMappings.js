import { Asset } from 'expo-asset';

const imageMappings = {
  './images/wide_grip_barbell_curl.jpg': Asset.fromModule(require('../assets/images/wide_grip_barbell_curl.jpg')).uri,
  // Add other image mappings here
};

export default imageMappings;
